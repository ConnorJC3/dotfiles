---
- defaults:
    link:
      create: true
      relink: true
      force: true

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.config/neofetch:
    ~/.config/paru:

- shell:
    - [git config --global --add --bool push.autoSetupRemote true, Enable git autoSetupRemote]
