---
- defaults:
    link:
      create: true
      relink: true
      force: true

- pacman:
    - pigz
    - pbzip2

- link:
    ~/.config/pacman:
    ~/.config/paru:

- shell:
    - [sudo cp ./root-config/pacman.conf /etc/pacman.conf, Installing pacman config]
